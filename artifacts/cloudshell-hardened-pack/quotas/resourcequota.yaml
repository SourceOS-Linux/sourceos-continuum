apiVersion: v1
kind: ResourceQuota
metadata:
  name: rq-devtools
  namespace: devtools
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    persistentvolumeclaims: "50"
    requests.storage: 200Gi
