apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudshell.fullname" . }}-shared
  labels:
    app.kubernetes.io/name: cloudshell
spec:
  selector:
    app.kubernetes.io/name: cloudshell
    cloudshell/mode: shared
  ports:
  - name: http
    port: 80
    targetPort: http
